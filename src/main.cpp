#include "log.h"

int main(int argc, char* argv[]) {
    Log::v("main", "-----   start medici process.   -----");
    
    
    Log::v("main", "----- terminate medici process. -----");
    return 0;
}
